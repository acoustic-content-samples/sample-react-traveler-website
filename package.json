{
  "name": "acoustic-website-react-sample",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "homepage": "/00000000-0000-0000-0000-00000000000/samples/traveler-website",
  "dependencies": {
    "@material-ui/core": "4.6.0",
    "@material-ui/icons": "4.5.1",
    "react": "16.11.0",
    "react-dom": "16.11.0",
    "react-image-gallery": "1.0.3",
    "react-router-dom": "5.1.2",
    "react-scripts": "3.3.0"
  },
  "devDependencies": {
    "node-sass": "4.13.1",
    "shelljs": "0.8.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "hub-build": "npm run build && node copyBuildFiles",
    "hub-deploy": "wchtools push -A -v -I --dir build",
    "hub-build-deploy": "npm run hub-build && npm run hub-deploy",
    "init-content": "wchtools push -A -I -f -v --dir content-artifacts",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
